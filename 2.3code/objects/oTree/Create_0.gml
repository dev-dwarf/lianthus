/// @description

// Inherit the parent event
event_inherited();

sprite_index = choose(sTree1, sTree2);

mask_index = sTree1;

//image_angle = irandom(359)

has_birds = chance(50);
birdtimer = 10;
wobble = 0;